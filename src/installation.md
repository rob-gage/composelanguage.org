# Installation

The Compose interactive environment, Compositor, can be installed easily with [Cargo](https://doc.rust-lang.org/stable/cargo/).

`cargo install compositor`

To start the interactive environment, use command `cmpstr`, and to exit from within the environment, use `!exit`.
